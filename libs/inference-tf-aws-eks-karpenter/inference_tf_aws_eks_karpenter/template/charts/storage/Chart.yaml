apiVersion: v2
name: storage
# SemVer spelling of the template version 0.1.0rc1 (Helm requires SemVer; PEP 440
# 'rc1' == SemVer '-rc1'). Kept in sync with the template version.
version: 0.1.0-rc1
description: First-party StorageClasses (EBS gp3 default) + the S3-mount static PV/PVC for the inference cluster
type: application
